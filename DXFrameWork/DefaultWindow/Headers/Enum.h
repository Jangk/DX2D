#pragma once

enum DISPLAY_MODE
{
	MODE_FULL,
	MODE_WIN
};

enum TEXTURE_TYPE
{
	TEXTURE_SINGLE,
	TEXTURE_MULTI
};

enum OBJECT_TYPE
{
	OBJECT_TERRAIN,
	OBJECT_PLAYER,
	OBJECT_MONSTER,
	OBJECT_EFFECT,
	OBJECT_UI,
	OBJECT_END
};

enum SCENE_TYPE
{
	SCENE_LOGO,
	SCENE_STAGE,
	SCENE_END
};